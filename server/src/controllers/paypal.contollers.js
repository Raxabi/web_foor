/**
 * * This function render the page of the login at if the user will proceed with the payment
 * * and not have an active session
 */

export const renderPayPalLogin = (req, res) => {
    
};

/**
 * * This function render the page of the payment at the moment of the payment 
 */

export const renderPayPalPayment = (req, res) => {

};

/**
 * * This function render the page of the canceled payment when a payment is canceled before the pay  
 */

export const renderPayPalCancelPayment = (req, res) => {

};